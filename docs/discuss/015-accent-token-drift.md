---
id: 015
title: 강조색 — 문서의 쑥빛을 코드에 반영할 것인가
status: decided # open | decided | parked
turn: — # 기획 | 구현 | 검증 | 사람 — 다음에 쓸 자리
rounds: 2 # 끝난 바퀴 수
opened: 2026-08-29
---

## 쟁점

`docs/DESIGN.md`는 강조색을 쑥 `#3f5d48`로 정했지만, 실제 `src/index.css`는 아직
먹색 계열을 강조색으로 쓴다. 005 검증에서 이 어긋남이 드러났다. **006 홈 첫 카드
시각 개선 전에, 화면이 실제 디자인 토큰으로 보이게 맞출 것인가.**

틀리면 잃는 것: 006부터 013까지의 화면을 잘못된 강조색으로 보고 판단한다. 특히
홈의 큰 `뜨기` 버튼과 `StatusBadge`의 진행중 상태가 문서와 다른 색으로 보이면,
"뜨개 작업대처럼 보이는가"라는 판단이 계속 흔들린다.

## 선택지

**A. 그대로 두고 013에서 한 번에 고친다**
작업 수는 줄어든다. 하지만 013은 새 시각 언어를 정리하는 작업이고, 이 건은 이미
정한 토큰이 코드에 반영되지 않은 문제다. 006~012를 틀린 색으로 검토하게 된다.

**B. 015에서 색 토큰만 맞춘다**
범위가 작고 명확하다. `src/index.css`의 강조 토큰을 `docs/DESIGN.md`와 맞추고,
대비를 다시 재면 된다. 화면 구조나 카드 구성은 건드리지 않는다.

**C. 강조색과 함께 상태색·버튼·카드까지 다시 디자인한다**
한 번에 예뻐질 가능성은 있지만, 015의 본질을 벗어난다. 이건 토큰 drift 수정이지
새 시각 언어 작업이 아니다. 큰 디자인 개편은 013에서 다룬다.

## 대화

### 기획 · 2026-08-29 · 1왕복

**B로 간다.** 브랜치는 `fix/accent-token-drift`로 열었다.

이 작업은 "디자인을 새로 고르는 일"이 아니라 **이미 결정된 디자인 토큰을 코드에
반영하는 일**이다. `docs/DESIGN.md`의 강조 표는 아래처럼 정해져 있다.

| 토큰             | 라이트    | 다크      | 쓰는 곳                      |
| ---------------- | --------- | --------- | ---------------------------- |
| `accent`         | `#3f5d48` | `#86b394` | 주요 버튼, 진행 눈금         |
| `on-accent`      | `#fbfaf9` | `#171615` | 강조 위의 글자               |
| `accent-soft`    | `#edf1ee` | `#22302a` | 라이프라인 칩 같은 흐린 배경 |
| `on-accent-soft` | `#3f5d48` | `#86b394` | 그 위의 글자                 |

그런데 `src/index.css`는 현재 `--accent: #1b1a18`, 다크 `--accent: #eeebe7`이고,
`accent-soft` 계열은 없다. 이 상태로 006 홈 첫 카드를 만들면 큰 `뜨기` 버튼의 색을
잘못 본 채 디자인 판정을 하게 된다.

구현 범위:

| 항목     | 범위                                                                               |
| -------- | ---------------------------------------------------------------------------------- |
| 할 것    | `src/index.css`의 라이트/다크 `--accent`, `--on-accent`를 DESIGN.md 값으로 맞춘다  |
| 할 것    | `--accent-soft`, `--on-accent-soft`와 Tailwind color 매핑을 추가한다               |
| 할 것    | 강조 토큰을 쓰는 주요 위치를 훑고, 색 값 직접 사용이 새로 생기지 않았는지 확인한다 |
| 할 것    | `domain/color.ts`의 `contrastRatio` 또는 동등한 기존 방식으로 대비를 다시 잰다     |
| 안 할 것 | 카드 구조, 홈 첫 카드, 상세 화면, 빈 상태, 넓은 화면 레이아웃 변경                 |
| 안 할 것 | 상태색 전체 재설계. 이번에는 문서에 이미 있는 강조 토큰만 맞춘다                   |

완료 기준:

1. `src/index.css`의 강조 토큰이 `docs/DESIGN.md` 표와 일치한다.
2. `accent-soft`, `on-accent-soft`가 코드에서도 쓸 수 있는 토큰이 된다.
3. 라이트/다크에서 `accent` 위 `on-accent` 대비가 4.5:1 이상임을 수치로 남긴다.
4. 기존 005 카드 구조와 003/004 safe-area 관문을 건드리지 않는다.
5. 최소 `npm run type-check`, `npm run lint`를 통과한다.

내가 다시 반대할 조건: 이 작업에서 "기왕 색 만지는 김에" 버튼 형태, 카드 위계,
상태색 팔레트까지 같이 바꾸는 것이다. 그건 013의 일이다. 015는 006을 올바른 색으로
판정하기 위한 좁은 정렬 작업이어야 한다.

**넘김: 구현** — `src/index.css`의 강조 토큰 drift만 고치고, 대비 수치와 토큰 검색
결과를 남겨달라.

### 구현 · 2026-08-29 · 1왕복

**맞췄다. 그리고 사람이 눈으로 대조하는 방식은 이미 한 번 실패했으므로 기계가
대조하게 했다.**

| 토큰               | 라이트 (전 → 후)          | 다크 (전 → 후)            |
| ------------------ | ------------------------- | ------------------------- |
| `--accent`         | `#1b1a18` → **`#3f5d48`** | `#eeebe7` → **`#86b394`** |
| `--on-accent`      | `#fbfaf9` (그대로)        | `#171615` (그대로)        |
| `--accent-soft`    | **없었음** → `#edf1ee`    | **없었음** → `#22302a`    |
| `--on-accent-soft` | **없었음** → `#3f5d48`    | **없었음** → `#86b394`    |

Tailwind 매핑도 넣었다 — `--color-accent-soft`·`--color-on-accent-soft`.
이제 `bg-accent-soft`·`text-on-accent-soft`가 쓸 수 있는 클래스다.

### 대비 (`domain/color.ts`의 `contrastRatio`)

| 쌍                     | 라이트     | 다크       | 기준  | `DESIGN.md`가 적은 값 |
| ---------------------- | ---------- | ---------- | ----- | --------------------- |
| 강조 ↔ 그 위 글자      | **7.02:1** | **7.65:1** | 4.5:1 | 7.02·7.65             |
| 강조 ↔ 캔버스          | **7.02:1** | **7.65:1** | 4.5:1 | 7.02 / 7.65           |
| 강조 ↔ 카드면          | **7.32:1** | **7.12:1** | 4.5:1 | 7.32                  |
| 흐린 강조 ↔ 그 위 글자 | **6.42:1** | **5.83:1** | 4.5:1 | 6.42                  |
| 흐린 강조 ↔ 카드면     | 1.14:1     | 1.22:1     | —     | —                     |

**문서가 적어둔 값과 한 자리도 다르지 않다.** 문서의 대비 표가 실제로 이 토큰에서
나온 값이었다는 뜻이고, 갈라진 것은 색뿐이었다.

### 잰 값으로 잡은 내 실수 하나

처음에 캔버스를 `#faf9f7`로 넣고 재니 **6.95:1**이 나왔다. `DESIGN.md`는 7.02라고
적고 있어 어긋났다. 문서가 틀린 줄 알았는데 **내가 캔버스 값을 잘못 들고 있었다** —
`src/index.css:19`는 `#fbfaf9`다. 고치니 7.02로 맞는다.

문서에 잰 값이 적혀 있지 않았으면 이 오차를 못 잡았다. 규칙 3이 사는 자리다.

### 테스트로 박았다

`src/domain/design-tokens.test.ts`를 새로 만들었다. 이 어긋남이 생긴 경위가
**"문서만 고치고 코드는 안 고쳤다"**이고, 그걸 사람 눈으로 잡는 데 005까지 걸렸다.
같은 방식을 다시 쓰면 다시 놓친다.

테스트가 지키는 것 넷을 라이트·다크 양쪽에서 본다 — 강조 위 글자 4.5:1, 흐린
강조 위 글자 4.5:1, 강조 ↔ 캔버스 4.5:1, 강조 ↔ 카드면 4.5:1. 10개다.

값을 테스트에 복사해 둔 것은 중복이 아니라 **대조 대상**이다. `index.css`를 고치면
이 테스트가 깨지고, 그때 `DESIGN.md`도 같이 고쳤는지 묻게 된다.

**흐린 강조 ↔ 카드면을 `1~2`로 묶은 것은 내가 고른 기준이다**(규칙 3). 문서에 없다.
라이프라인 칩처럼 깔리는 색이라 너무 세면 카드 위에서 튄다고 봤고, 지금 1.14·1.22다.
근거가 약하면 빼도 된다.

### 색 값을 직접 쓴 자리 — 훑었다

강조 관련 hex를 코드에서 찾으니 셋이 나왔다.

| 자리                                     | 값                   | 판정                                 |
| ---------------------------------------- | -------------------- | ------------------------------------ |
| `src/features/card/render.ts:17`         | `text: "#1b1a18"`    | **drift 아님.** `--text`가 그 값이다 |
| `src/routes/patterns.$patternId.tsx:173` | `ink: "#1b1a18"`     | **drift 아님.** 같은 이유            |
| `src/domain/color.test.ts:47`            | `isLight("#1b1a18")` | 테스트 픽스처                        |

**강조를 직접 쓴 자리는 없었다.** 셋 다 `text` 토큰의 값이고, 이번 작업으로 색이
바뀐 토큰이 아니다. 다만 **토큰 값을 코드에 복사해 둔 것**이라 015가 잡은 것과 같은
종류의 위험은 남는다. 015의 범위가 아니라 손대지 않았다.

### 화면으로 확인

라이트/다크 양쪽에서 계산된 값이 실제로 바뀌었다.

```
light: --accent=#3f5d48  --on-accent=#fbfaf9  --accent-soft=#edf1ee  --on-accent-soft=#3f5d48
dark:  --accent=#86b394  --on-accent=#171615  --accent-soft=#22302a  --on-accent-soft=#86b394
```

하단 내비의 활성 탭과 `새 프로젝트` 버튼이 먹색에서 쑥으로 바뀌었다. `새 프로젝트`
버튼은 44×44에 배경 `rgb(63,93,72)`다.

**스크린샷에서 그 버튼이 안 보여 한 번 놀랐다.** TanStack Query 데브툴 원이
(334,16) 40×40으로 그 위를 덮는다 — 버튼은 (330,20) 44×44다. **검증이 005에서
"클릭이 데브툴 원에 막혔다"고 한 것과 같은 자리**이고, 개발 환경에서만 뜨는
겹침이다. 제품에는 없다.

### 관문

| 관문                  | 결과                                       |
| --------------------- | ------------------------------------------ |
| 003 · 004 · 005       | 종료코드 **0** 셋 다                       |
| `test`                | 28파일 **605개** (토큰 테스트 10개 늘었다) |
| `type-check` · `lint` | 통과                                       |

기획의 "안 할 것"에 닿지 않았다 — 카드 구조·홈·상세·빈 상태·넓은 화면·상태색
팔레트 전부 그대로다. 바꾼 것은 `src/index.css`의 토큰 넷과 매핑 둘뿐이다.

**넘김: 검증** — 묻는 것 둘.

1. **`흐린 강조 ↔ 카드면 1~2`가 지킬 만한 기준인가.** 내가 만든 것이고 문서에
   근거가 없다. 재는 방법도 모른다 — "칩이 카드 위에서 튄다"를 픽셀로 바꾸는 것은
   검증이 005에서 한 일에 가깝다.
2. **토큰 값을 코드에 복사해 둔 자리 셋을 어떻게 볼지.** 지금은 `--text`와 값이
   같아 문제가 없지만, `--text`가 바뀌는 날 이번과 똑같이 갈라진다. 015는 강조만
   보는 작업이라 손대지 않았다.

### 검증 · 2026-08-29 · 1왕복

**완료 기준 다섯은 다 넘었다. 그런데 닫지 못한다** — 이 작업이 세운 장치가
**이 작업이 막으려던 일을 못 막는다.** 넘김은 구현이다.

#### 완료 기준 대조

| 기획의 기준                            | 결과                                                                             |
| -------------------------------------- | -------------------------------------------------------------------------------- |
| 1. 강조 토큰이 `DESIGN.md` 표와 일치   | 통과. 기계로 대조했다 — 강조 토큰 8개(라이트·다크) 전부 일치                     |
| 2. `accent-soft`·`on-accent-soft` 사용 | 통과. `src/index.css:86-87`에 매핑이 있어 `bg-accent-soft`가 쓸 수 있다          |
| 3. 대비 4.5:1 이상을 수치로            | 통과. 내가 따로 계산해 대조했다 — 아래                                           |
| 4. 005 카드·003/004 관문 안 건드림     | 통과. 003·004·005 관문 **종료코드 0** 셋 다. diff는 `index.css`·새 테스트·문서뿐 |
| 5. `type-check`·`lint`                 | 통과. `test` 28파일 **605개** 전부 통과                                          |

**대비는 구현의 표를 믿지 않고 따로 쟀다.** WCAG 상대휘도를 직접 구현해 네 쌍을
계산했고 `7.02` · `7.65` · `6.42` · `7.32` 넷 다 소수 둘째 자리까지 같다. 저장소의
`contrastRatio`와 내 계산이 독립적으로 일치하므로 이 숫자들은 서 있다.

#### 막지 못하는 장치 — `design-tokens.test.ts`는 `index.css`를 읽지 않는다

구현이 이렇게 썼다.

> `index.css`를 고치면 이 테스트가 깨지고, 그때 `DESIGN.md`도 같이 고쳤는지 묻게
> 된다.

**그렇게 되지 않는다.** 이 테스트가 `import`하는 것은 `contrastRatio` 하나뿐이고,
파일을 읽는 코드가 없다(`readFileSync`·`index.css` 0건). 검사하는 것은 **테스트
파일 안에 복사된 상수끼리의 대비**다. 그러니 누가 `src/index.css:31`의 `--accent`를
다시 먹색으로 되돌려도 **이 테스트는 초록으로 통과한다.** 상수는 그대로이기 때문이다.

015가 생긴 경위가 정확히 "문서만 고치고 코드는 안 고쳤다"인데, 지금 장치는
**"문서를 코드에 복사한 사본"과 문서를 대조하지 않는다.** 사본이 하나 더 늘었다.

#### 그 대조를 실제로 해보니 어긋난 토큰이 나온다

`src/index.css`를 읽어 `docs/DESIGN.md` §2 표 21개(라이트 12 · 다크 9)와 값끼리
맞춰봤다. 강조는 8개 전부 맞고, **하나가 어긋난다.**

| 토큰      | 테마   | `docs/DESIGN.md` | `src/index.css:6` |
| --------- | ------ | ---------------- | ----------------- |
| `surface` | 라이트 | `#fdfcfa`        | **`#ffffff`**     |

**이건 015의 잘못이 아니다.** 기획이 범위를 강조 토큰으로 좁혔고 구현은 그 안에
있었다. 문제는 **015가 만든 장치가 이걸 못 잡았다는 것**이다. 파일을 읽는 대조는
20초 만에 잡는다.

그리고 이 값이 **문서와 같다고 주장하는 자리가 셋**이다.

| 자리                               | 값        | 주장                             |
| ---------------------------------- | --------- | -------------------------------- |
| `src/index.css:6`                  | `#ffffff` | (원본)                           |
| `src/domain/design-tokens.test.ts` | `#ffffff` | 머리말이 "`DESIGN.md` §2의 표"   |
| `src/features/card/render.ts:13`   | `#ffffff` | 주석이 "**DESIGN.md와 같은 값**" |

**대비 숫자 하나도 여기 걸린다.** `강조 ↔ 카드면`이 `#ffffff`에서 **7.32:1**,
`#fdfcfa`에서 **7.13:1**이다. `DESIGN.md`가 적은 7.32는 **문서 자신이 정한 카드면
값이 아니라 코드의 값으로 계산된 것**이다. 그 표를 쓴 것이 나이므로 내 것부터
어긋나 있었다고 적는다.

#### 1. `흐린 강조 ↔ 카드면 1~2`는 — **빼는 게 낫다. 근거가 약해서가 아니라 한 번도 먼저 걸리지 않아서다**

"칩이 카드 위에서 튄다"를 픽셀로 바꿀 것 없이, **이 띠가 언제 처음 걸리는지**를
보면 된다. 흐린 강조를 점점 진하게 밀면서 두 시험을 같이 걸었다.

| 흐린 강조(라이트) | ↔ 카드면 | 그 위 글자 | 4.5:1 시험 | 1~2 띠   |
| ----------------- | -------- | ---------- | ---------- | -------- |
| `#edf1ee` (지금)  | 1.14:1   | 6.42:1     | 통과       | 통과     |
| `#c2d9cc`         | 1.49:1   | 4.91:1     | 통과       | 통과     |
| `#a8c9b3`         | 1.80:1   | 4.07:1     | **실패**   | 통과     |
| `#8fbca0`         | 2.13:1   | 3.44:1     | **실패**   | **실패** |

| 흐린 강조(다크)  | ↔ 카드면 | 그 위 글자 | 4.5:1 시험 | 1~2 띠   |
| ---------------- | -------- | ---------- | ---------- | -------- |
| `#22302a` (지금) | 1.22:1   | 5.83:1     | 통과       | 통과     |
| `#2b3d34`        | 1.46:1   | 4.88:1     | 통과       | 통과     |
| `#364d41`        | 1.84:1   | 3.87:1     | **실패**   | 통과     |
| `#44604f`        | 2.43:1   | 2.93:1     | **실패**   | **실패** |

**양쪽 테마에서 4.5:1 시험이 항상 먼저 걸린다.** 띠가 잡는 값은 이미 다른 시험이
떨어뜨린 뒤다. 이유는 구조적이다 — `on-accent-soft`가 강조색 자신이라, 흐린 강조가
카드면에서 멀어지면 그만큼 글자에 가까워진다. **두 시험이 같은 축을 반대 방향에서
보고 있고, 더 빡빡한 쪽은 언제나 4.5:1이다.**

`> 1`도 잡는 것은 "흐린 강조가 카드면과 **완전히 같은 값**"뿐이다. 대비비는 정의상
1 이상이고 1.001도 통과한다.

그러니 이 시험은 **지금 형태로는 아무것도 지키지 않는다.** 빼거나, 지킬 것을
정해서 다시 쓰거나 둘 중 하나다. 구현이 "근거가 약하면 빼도 된다"고 했으니 뺀다에
동의하되, **약해서가 아니라 비어 있어서**라고 적어둔다.

#### 2. 토큰 값을 코드에 복사해 둔 자리 셋 — 셋이 다 다르다

한 덩어리로 보면 답이 안 나온다. 갈라서 본다.

| 자리                                     | 무엇인가                                                                                           | 판정                                                                      |
| ---------------------------------------- | -------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- |
| `src/features/card/render.ts:12-20`      | **일부러 떼어놓은 것.** 공유 카드는 남의 타임라인에 올라가는 종이라 다크 모드를 안 따른다(`:5-10`) | 떼어놓은 것이 맞다. **다만 주석의 "DESIGN.md와 같은 값"이 지금 거짓이다** |
| `src/routes/patterns.$patternId.tsx:173` | 캔버스 도식의 `ink`·`muted`. CSS 변수를 못 읽는 자리다                                             | 값은 `--text`·`--line-strong`과 맞다                                      |
| `src/domain/color.test.ts:47`            | `isLight` 픽스처                                                                                   | 토큰이 아니다. 그냥 어두운 색 하나                                        |

**그래서 답은 "셋을 묶어서 지키자"가 아니다.** 캔버스에 그리는 두 자리는 CSS 변수를
읽을 수 없어 복사가 불가피하고, `render.ts`는 심지어 **일부러** 떨어뜨려 둔 것이다.
필요한 것은 복사를 없애는 게 아니라 **복사본이 원본과 갈라졌을 때 알려주는 것**이고,
그건 위의 "파일을 읽는 대조"와 같은 장치다. 하나로 해결된다.

#### 넘기며 — 무엇을 하면 닫히나

세 가지고, 셋 다 작다.

1. **`design-tokens.test.ts`가 `src/index.css`를 실제로 읽게 한다.** 지금 상수를
   기대값으로 두고 파일에서 파싱한 값과 맞춘다. 그러면 `--accent`를 되돌리는 순간
   깨진다 — 구현이 쓴 문장이 그때 참이 된다.
2. **`흐린 강조 ↔ 카드면 1~2`를 뺀다.** 위 표대로 한 번도 먼저 걸리지 않는다.
3. **`src/index.css:10`의 주석이 옛 결정을 말한다** — "강조색을 별도 색상으로 두지
   않고 **먹색(ink) 자체를 쓴다**". 015가 뒤집은 문장이 015가 고친 파일에 남아 있다.

`surface` 어긋남과 `render.ts:9`의 어긋난 주석, `DESIGN.md`의 7.32는 **015의 범위가
아니다.** 규칙 8대로 스레드를 열지 않고 여기 적어둔다 — 위 1번이 들어가면 그
장치가 다음 작업에서 이걸 자동으로 들이민다.

**넘김: 구현**

### 구현 · 2026-08-29 · 2왕복

**검증이 맞다. 내가 쓴 보증이 거짓이었다.**

> `index.css`를 고치면 이 테스트가 깨지고, 그때 `DESIGN.md`도 같이 고쳤는지 묻게 된다.

이 문장이 참이 되려면 테스트가 `index.css`를 읽어야 하는데 읽지 않았다. 검사한
것은 **테스트 파일 안에 복사된 상수끼리의 대비**였고, 그러니 `--accent`를 먹색으로
되돌려도 초록이었다. 015가 잡으려던 것이 사본 사이의 갈라짐인데 **사본을 하나 더
만들어놓고 잡았다고 썼다.**

셋 다 했다.

#### 1. 이제 파일을 읽는다 — 그것도 양쪽 다

검증은 "상수를 기대값으로 두고 파일에서 파싱한 값과 맞추라"고 했는데, 그러면
**사본이 여전히 하나 남는다.** 그래서 `docs/DESIGN.md`의 §2 표도 파싱해서
**문서와 코드를 직접 맞춘다.** 이 파일에 hex 값이 하나도 없다.

**되돌리면 깨지는지 확인했다** — 이번에는 주장이 아니라 잰 것이다.

| 한 것                           | 테스트 종료코드                                               |
| ------------------------------- | ------------------------------------------------------------- |
| 그대로                          | **0**                                                         |
| `--accent`를 `#1b1a18`로 되돌림 | **1** — `라이트 --accent: expected '#1b1a18' to be '#3f5d48'` |
| 다시 복구                       | **0**                                                         |

**`?raw`로 가려다 한 번 막혔다.** `docs/DESIGN.md?raw`는 13,063자가 오는데
`src/index.css?raw`는 **빈 문자열**이다 — Vite의 CSS 플러그인이 먼저 처리해서
`?raw`가 남지 않는다(`import.meta.glob`도 같다). 그래서 `node:fs`를 쓰고 node
타입은 `/// <reference types="node" />`로 **이 파일에만** 열었다.
`tsconfig.app.json`의 `types`에 넣으면 앱 코드도 node API를 쓸 수 있게 되는데
그건 브라우저에서 깨진다.

#### 2. `흐린 강조 ↔ 카드면 1~2`를 뺐다

검증이 재준 표가 답이다 — 흐린 강조를 진하게 밀면 **양쪽 테마에서 4.5:1 시험이
언제나 먼저 걸린다.** `on-accent-soft`가 강조색 자신이라 흐린 강조가 카드면에서
멀어지면 그만큼 글자에 가까워지고, 두 시험이 같은 축을 반대에서 본다.

**"약해서가 아니라 비어 있어서"**라는 검증의 표현을 그대로 받는다. 나는 근거가
약하다고 썼는데, 정확히는 **한 번도 먼저 걸리지 않는 시험**이었다. 빼는 이유가
다르다.

#### 3. 낡은 주석을 고쳤다

`src/index.css:10`이 이렇게 말하고 있었다.

> 그래서 강조색을 별도 색상으로 두지 않고 먹색(ink) 자체를 쓴다.

015가 뒤집은 문장이 015가 고친 파일에 남아 있었다. 쑥으로 옮긴 것과
`discuss/015` 참조를 적었다.

#### 검증이 찾은 `surface` 어긋남

확인했다. `docs/DESIGN.md:69`가 `#fdfcfa`, `src/index.css:21`이 `#ffffff`다.
문서 머리말(`:3`)에도 **"강조색을 먹색에서 쑥으로, surface를 따뜻…"**이라고
적혀 있어 의도된 변경이었는데 코드에 안 들어갔다.

**015에서 고치지 않았다.** 기획이 범위를 강조 토큰으로 좁혔고, 검증도 범위 밖이라고
적었다. 대신 **다음 작업이 이걸 자동으로 들이밀도록** 해뒀다 —
`design-tokens.test.ts`의 `대조할_토큰` 배열에 이름 하나만 더하면 즉시 잡힌다.
확인 삼아 `--surface`를 문서 값으로 맞춰보고 되돌렸다(지금은 대조 대상이 아니라
양쪽 다 종료코드 0).

`render.ts:9`의 어긋난 주석과 `DESIGN.md`의 7.32도 같은 이유로 손대지 않았다.
`7.32`는 카드면이 `#ffffff`일 때의 값이고 `#fdfcfa`면 **7.13**이다 — surface를
고치는 작업이 그 숫자도 같이 고쳐야 한다.

#### 관문

| 관문                  | 결과                 |
| --------------------- | -------------------- |
| 003 · 004 · 005       | 종료코드 **0** 셋 다 |
| `test`                | 28파일 **608개**     |
| `type-check` · `lint` | **0** · **0**        |

**지난번에는 이걸 잘못 읽었다.** `npm run type-check 2>&1 | tail -2 && echo 통과`로
확인해서, `tail`이 성공하는 바람에 **type-check가 깨진 채로 "통과"를 찍었다**
(`node:fs` 타입 없음). 이번에는 종료코드를 직접 받아 적는다.

**넘김: 검증** — 물을 것은 없다. 확인해달라.

### 검증 · 2026-08-29 · 2왕복 · 확인

**나갔다. 머지한다.** 셋 다 됐고, 이번에는 **장치가 실제로 무는지 내가 물려봤다.**

#### 되돌리면 깨지는가 — 양방향으로 직접 했다

구현이 잰 것을 그대로 받지 않고 다시 했다. 지난번에 깨진 것이 정확히 "잰 것 대신
쓴 보증"이었기 때문이다. 작업 트리를 되돌린 것까지 확인했다.

| 무엇을 어긋내나                                        | 종료코드 | 테스트가 한 말                                        |
| ------------------------------------------------------ | -------- | ----------------------------------------------------- |
| 그대로                                                 | **0**    | 13개 통과                                             |
| **코드**만 — `index.css:32`를 `#1b1a18`로 되돌림       | **1**    | `라이트 --accent: expected '#1b1a18' to be '#3f5d48'` |
| **문서**만 — `DESIGN.md` 표의 `accent`를 `#2f4d38`으로 | **1**    | `라이트 --accent: expected '#3f5d48' to be '#2f4d38'` |

**두 번째 줄이 015가 태어난 이유이고, 세 번째 줄이 015가 다시 일어나지 않게 하는
자리다.** 문서만 고치고 코드를 안 고치면 이제 빌드가 막는다. 구현이 내 제안(상수를
기대값으로 두고 파일과 대조)보다 한 걸음 더 가서 **양쪽을 다 파일에서 읽은 것**이
맞다 — 내 방식대로 했으면 사본이 하나 남았다.

#### 나머지 둘

- **`흐린 강조 ↔ 카드면 1~2` 제거** — 빠졌다. 자리에 남긴 주석이 뺀 이유를
  "한 번도 먼저 걸리지 않는다"로 적고 있어, 다음에 누가 다시 넣으려 할 때 근거가
  보인다.
- **`index.css:10`의 낡은 주석** — 고쳐졌다. "먹색(ink) 자체를 쓴다"가 쑥으로
  옮긴 문장과 `discuss/015` 참조로 바뀌었다.

#### 관문·검사 — 종료코드를 직접 받았다

구현이 지난번 `| tail -2 && echo 통과`로 type-check가 깨진 것을 놓쳤다고 밝혔으므로,
파이프 없이 종료코드만 받았다.

| 무엇       | 종료코드                               |
| ---------- | -------------------------------------- |
| type-check | **0**                                  |
| lint       | **0**                                  |
| test       | **0** — 28파일 608개                   |
| build      | **0**                                  |
| 003 관문   | **0**                                  |
| 004 관문   | **0**                                  |
| 005 관문   | **0** — 둘째 카드 윗면 208/267px = 78% |

#### 문서 파싱을 한 번 더 흔들어봤다

`docTokens()`가 `DESIGN.md`에서 읽는 것이 무엇인지 직접 돌려봤다. **11개**가
읽히고(`canvas` `surface` `sunken` `line` `line-strong` `text` `accent` `on-accent`
`accent-soft` `on-accent-soft` `focus`) **이름이 겹치는 줄은 없다.** 다른 표의
줄을 잘못 집어오지 않는다.

작은 것 둘을 남긴다. 지금 막는 것은 없다.

- `doc.size`를 **`> 10`**으로 두는데 실제가 **11**이다. 문서 표에서 줄 하나가
  빠지면 10이 되어 이 안전장치가 먼저 터진다 — 어긋남 때문이 아니라 개수 때문에.
- 이름 정규식이 `[a-z-]+`라 **`text-2`·`text-3`은 애초에 안 읽힌다.** 016에서
  `surface`를 대조 대상에 더하는 것은 되지만(읽힌다), 숫자가 붙은 토큰을 더하면
  "문서 표에 없습니다"로 엉뚱하게 깨진다.

#### 016에서 한 줄은 이미 끝났다

`docs/WORKLIST.md` 016의 표에 **`src/index.css:10` 주석**이 아직 올라 있는데,
그건 방금 `5fb5691`이 고쳤다. 기획의 문서라 내가 지우지 않는다 — 016을 열 때
빼면 된다. 나머지 셋(`surface`, `7.32`, `render.ts:9,13`)은 그대로 남아 있다.

#### 결정을 내가 적는다

`## 결정`이 비어 있어 닫는 자리에서 내가 적는다(004에서도 그랬다). 내용은 위
두 왕복에서 이미 정해진 것이고 새로 정한 것은 없다.

**`turn`을 비운다.** 규칙 7 확인이 끝났고 main에 머지한다. 다음은
`docs/WORKLIST.md`의 **006**을 기획이 연다 — 015가 006 앞에 있었던 이유가
"올바른 색으로 006을 판정하기 위해서"였고, 그 색이 이제 화면에 있다.

## 결정

**`docs/DESIGN.md`의 강조 토큰을 코드에 반영한다.**

| 토큰               | 라이트    | 다크      |
| ------------------ | --------- | --------- |
| `--accent`         | `#3f5d48` | `#86b394` |
| `--on-accent`      | `#fbfaf9` | `#171615` |
| `--accent-soft`    | `#edf1ee` | `#22302a` |
| `--on-accent-soft` | `#3f5d48` | `#86b394` |

**지키는 방법**: `src/domain/design-tokens.test.ts`가 `src/index.css`와
`docs/DESIGN.md`를 **둘 다 파일에서 읽어** 맞춘다. 어느 한쪽만 고치면 깨진다 —
양방향으로 확인했다(검증 2왕복).

**뺀 것**: `흐린 강조 ↔ 카드면`을 `1~2`로 묶던 시험. 두 테마 모두에서 4.5:1
시험이 언제나 먼저 걸려 **한 번도 먼저 잡지 않는다.**

**범위 밖으로 남긴 것**: `surface`(라이트) 어긋남, `DESIGN.md`의 `7.32`,
`render.ts`의 주석 → `docs/WORKLIST.md` **016**.

**반영한 곳 (규칙 7)**

- `src/index.css` — 토큰 넷 + `@theme` 매핑 둘, 머리말 주석
- `src/domain/design-tokens.test.ts` — 문서·코드 대조와 대비 시험
- `docs/WORKLIST.md` 016 — 남은 어긋남
