---
id: 007
title: 빈 상태 — 채울 것이 없을 때 무엇을 그리나
status: open # open | decided | parked
turn: 구현 # 기획 | 구현 | 검증 | 사람 — 다음에 쓸 자리
rounds: 0 # 끝난 바퀴 수
opened: 2026-09-19
---

## 쟁점

**데이터가 하나도 없을 때 화면에 무엇을 그리나.** 그리고 **아무것도 없다는 말을
한 화면에서 몇 번 하나.**

005는 목록 카드를 이미지형으로 바꿨고 006은 홈 첫 카드를 복귀 카드로 바꿨다. 둘
다 **데이터가 있는 사용자**의 화면이다. 첫날의 화면은 005 이전 그대로다.

`docs/BRIEF.md:81`이 이 문제를 이렇게 적었다.

> 이 앱은 사용자가 데이터를 넣어야 예뻐지는 구조인데, 첫날에는 데이터가 없다.

**틀리면 무엇을 잃나.** 셋이다.

1. **첫날에 앱을 닫는다.** 이 앱의 전제는 "중단은 실패가 아니다"이고
   (`AGENTS.md:9-11`), 가장 많이 중단하는 사람은 초보다(`docs/DESIGN.md:43-45`).
   빈 화면이 숙제로 읽히면 그 사람을 문 앞에서 돌려보낸다. 기능이 아니라 **첫
   인상 하나**로.
2. **반대쪽으로 틀리면 데이터가 조용히 썩는다.** `docs/DESIGN.md:49-50`이
   "미리 채워진 예시값은 빈칸보다 나쁘다 — 정답처럼 보여서 그대로 저장되고,
   그러면 이후 계산이 전부 조용히 틀린다"고 못 박았다. 빈 화면을 예쁘게 만들려고
   샘플을 진짜처럼 그리면 그 함정에 그대로 들어간다.
3. **이월 항목을 버린다.** `docs/WORKLIST.md:243-247`이 007의 완료 기준으로
   박아둔 것이 있다 — 사진도 실도 없는 프로젝트 카드가 목록에서 빈 면으로
   읽히는 문제. 005 기획이 그 자리에 **"007에서 또 미루면 미룬 것이 아니라 버린
   것이다"**라고 적었다(`005-project-image-cards.md:406-407`). 미룰 자리가 뒤에
   없다 — 008·009는 프로젝트 상세이고 013은 시각 언어다.

## 지금 화면에 있는 것 — 코드에서 센 것이다

**잰 것이 아니라 파일에 있는 것이다**(규칙 3). 006 기획이 같은 자리에서 같은
방식으로 적었다(`006-home-resume-card.md:48-50`).

| 무엇                        | 어디                                                 | 상태                                                                 |
| --------------------------- | ---------------------------------------------------- | -------------------------------------------------------------------- |
| 홈 빈 화면에 샘플 카드 없음 | `src/routes/index.tsx:54-71`                         | 점선 상자 + 문구 둘 + 버튼. 001 결정 8이 요구한 샘플 카드가 없다     |
| 첫날 홈에 결핍 블록 다섯    | `src/routes/index.tsx:118-136`                       | 값이 `0`인 통계 타일 셋                                              |
| 〃                          | `src/routes/index.tsx:371-372`                       | `아직 뜬 기록이 없어요` × 2 (이번 주·누적, `:140-161`에서 둘을 그림) |
| 목록 빈 상태에 버튼 없음    | `src/routes/projects.index.tsx:94-98`                | 문구 둘뿐. 새 프로젝트는 글자 없는 아이콘 버튼(`:59-67`)에만 있다    |
| 목록 빈 상태가 거짓말을 함  | `src/routes/projects.index.tsx:94`                   | 필터 결과 0과 데이터 0이 **같은 분기·같은 문구**다                   |
| 카드의 빈 윗면              | `src/features/photo/components/card-cover.tsx:63-68` | 356×267 면에 점선 96×64 하나 + 왼쪽 눈금(`:42-48`)                   |

**둘째 줄을 풀어 적는다.** 첫날(프로젝트 1개·계획중·사진 없음·실 없음·카운터
없음·세션 없음)의 홈에서 "없다"고 말하는 블록을 코드에서 세면 **5개**다 —
`아직 뜬 기록이 없어요` 둘과 값이 `0`인 타일 셋. 복귀 카드는 006 덕분에
제대로 나온다(`domain/stats.ts:157`이 계획중까지 본다). 그러니 첫날 홈은 **비어
있지 않은데 다섯 번 비었다고 말하는 화면**이다.

`.claude/skills/design-system/SKILL.md:60`은 이렇게 적었다.

> 한 화면에 빈 섹션이 셋 이상이면 그건 화면이 아니라 할 일 목록이다. 접어라.

**다섯째 줄이 문구로 거짓말을 한다.** `projects.index.tsx:94`는 `status` 필터가
걸려 있어도 결과가 0이면 `아직 프로젝트가 없어요`(`src/i18n/ui/ko.ts:63`)를
띄운다. 완성한 작품이 없어서 `완성` 필터가 빈 것뿐인데 **프로젝트가 없다고
말한다.** 사용자는 프로젝트 넷을 갖고 있다.

## 이월 항목에 정면으로 답한다

`docs/WORKLIST.md:243-247`이 남긴 것이고, 005가 미룬 이유가 그 안에 적혀 있다.

> 005에서 채우지 않은 이유는 001 결정 10이 조각 도면을 맨 뒤로 내렸기 때문이고,
> 도면 없이 그리면 001 결정 6이 금지한 장식 그래픽이 된다.

**그 이유는 지금도 그대로다. 그런데 미루는 답이 되지는 않는다** — 미룰 자리가
없기 때문이 아니라, **도면을 앞당겨도 이 칸은 안 채워지기 때문**이다.

001 결정 5(`001-hierarchy.md:314-315`)의 사슬이다.

> 대표 사진 → 조각 도면 → 게이지·단수 눈금 → 실 색 스와치 → 조용한 빈 상태
> (점선·눈금 구조, 큰 회색 면 아님)

**이 칸이 나오는 조건은 "사진도 실도 없다"가 아니라 "아무 데이터도 없다"이다.**
이름만 짓고 만든 직후의 프로젝트라 조각도, 게이지도, 카운터도 없다. 사슬의
윗단 셋을 전부 끌어와도 그릴 것이 없다. 005 기획이
`005-project-image-cards.md:392-393`에서 "지금 이 자리를 채우려면 없는 것을
발명해야 한다"고 쓴 것이 정확했고, **그 문장은 순서를 바꿔도 참이다.**

그래서 007이 할 일은 사슬을 앞당기는 것이 아니다. **001은 이 칸에 무엇이
오는지 이미 정해뒀다 — "점선·눈금 구조"다.** 실패한 것은 단의 선택이 아니라
그 단의 **밀도**다. 005 검증이 그 면의 채도를 0.012로 쟀고(`WORKLIST.md:246`),
화면으로 보면 큰 빈 면이라고 적었다(`005-project-image-cards.md:270-279`).

**그러니 "또 미루는가"에 대한 답은 아니오이고, 채우는 방법은 발명이 아니라
밀도다.** 그리고 밀도는 잴 수 있다 — 아래 관문 A가 그것이다.

## 선택지

### 목록 카드의 빈 윗면 (이월 항목)

**A. 지금 형태를 두고 문구만 손본다.**
가장 싸다. 포기하는 것: 이월 항목 자체다. 005 기획이 "두 번 미루면 버린
것"이라고 적어둔 그 결과가 된다. **고르지 않는다.**

**B. 조각 도면을 앞당긴다.**
001 결정 10(`001-hierarchy.md:322-323`)의 순서를 뒤집는다. 포기하는 것: 순서
자체보다 큰 것이 있다 — **이 카드에는 조각 데이터가 없어서 도면을 앞당겨도
윗면이 그대로 빈다.** 값을 치르고 아무것도 얻지 못한다.

**C. 카테고리·기법에서 오는 실루엣이나 패턴을 그린다.**
`docs/WORKLIST.md:98-99`가 005 방향으로 "실 색/카테고리 기반의 조용한 패턴이나
블록"이라고 쓴 적이 있다. 포기하는 것: UI가 **사용자 데이터가 아닌 그림**을
만들기 시작한다. `docs/DESIGN.md:130`이 색에 대해 "임의의 색을 UI가 만들어내지
않는다"고 한 것과 같은 자리이고, 그림체를 정하는 것은 013이다. 007이 먼저
그리면 013은 자기가 정하기 전에 이미 깔린 그림 위에서 시작한다.

**D. 사진도 실도 없으면 윗면을 접거나 줄인다.**
포기하는 것: 목록의 균일성. 005가 "없으면 안 그리기"를 쓰지 않은 이유가 이것이고
잰 근거가 있다 — 세 경우의 윗면 비율이 전부 1.33이라 카드 모양이 갈라지지
않는다(`005-project-image-cards.md:107-116`, `docs/DESIGN.md:224-228`).

**E. 윗면을 다음 행동의 자리로 쓴다** (`사진 추가` 같은 것).
`SKILL.md:56`의 빈 화면 규칙("무엇을 얻게 되는지 + 그리로 가는 버튼 하나")에
가장 가깝다. 포기하는 것: **001 결정 8**(`001-hierarchy.md:319-320`, 빈 상태는
화면당 하나). 그런 카드가 셋이면 목록이 할 일 목록이 된다(`SKILL.md:60`).

**F. 마지막 단을 제대로 만든다 — 고른다.**
001이 정한 "점선·눈금 구조"를 그대로 두되 **밀도를 관문으로 건다.** 포기하는
것을 정직하게 적는다 — **이 카드는 예뻐지지 않는다.** 조용한 구조가 될 뿐이고,
사진 있는 카드와의 인상 차는 남는다. 그 차를 없애는 길은 C뿐인데 그건 001이
막았다. **여기서 얻는 것은 "예쁨"이 아니라 "만들다 만 것이 아니라는 신호"다.**

### 홈의 빈 화면

**G. 지금처럼 문구 둘 + 버튼 하나.**
포기하는 것: 001 결정 8과 `WORKLIST.md:233`이 요구한 샘플 카드. 007 완료
기준의 "첫 사용자에게 앱의 최종 모습을 상상하게 한다"(`WORKLIST.md:240`)를
문구로는 넘을 수 없다. 문구는 최종 모습을 **설명**하고 샘플 카드는 **보여준다.**

**H. 006이 만든 복귀 카드의 모양을 점선 샘플 한 장으로 + CTA 하나 — 고른다.**
목록 카드가 아니라 **복귀 카드**를 샘플로 쓰는 이유: 홈의 빈 화면은 홈의 미래를
약속해야 한다. 001 결정 1(`:303-304`)이 홈 첫 카드를 상세 규칙으로 정했으므로
샘플도 그 모양이어야 한다.

포기하는 것: 샘플이 가짜 숫자를 하나 갖는다(단수). `docs/BRIEF.md:354-365`와
`docs/DESIGN.md:52-55`가 건 조건 셋 — 명백히 샘플로 보일 것, 누르면 저장이
아니라 만들기 화면으로 갈 것, **틀리면 손해가 나는 값(게이지·코수)을 쓰지 않을
것** — 을 전부 지킨다. 단수는 계산의 입력이 아니므로 셋째 조건에 걸리지 않는다.

**I. 샘플에 그럴듯한 편물 사진을 넣는다.**
**거부한다.** 001 결정 6(`:316-317`)이 "사진인 척하지 않는다"고 한 것과 같은
자리다. 샘플의 윗면은 **사진이 올 자리임을 말하는 구조**여야 하고, 편물을
흉내 내면 그 카드는 사용자의 작품인 척한다.

## 대화

### 기획 · 2026-09-19 · 1왕복

**F와 H로 간다.** 브랜치는 `feat/empty-states`로 열었다.

이 작업이 손대는 화면은 **둘뿐이다 — 홈과 프로젝트 목록.** 프로젝트 상세는
008·009의 자리이고, 007이 거기까지 가면 세 작업이 한 브랜치에 섞인다.

#### 007이 하는 일 넷

| 번호 | 무엇                | 어디                                                 |
| ---- | ------------------- | ---------------------------------------------------- |
| 1    | 홈 빈 화면의 샘플   | `src/routes/index.tsx:54-71`                         |
| 2    | 첫날 홈의 결핍 반복 | `src/routes/index.tsx:118-136` · `:371-372`          |
| 3    | 목록 빈 상태        | `src/routes/projects.index.tsx:94-98`                |
| 4    | 카드의 빈 윗면      | `src/features/photo/components/card-cover.tsx:63-68` |

**1 — 샘플은 점선이고 누르면 만들기로 간다.** 복귀 카드의 모양(사진 자리 ·
이름 · 단수 · `뜨기`)을 점선으로 한 장 그린다. 샘플임을 글로도 말한다. 위
선택지 I대로 **사진 자리는 구조로 두고 편물을 흉내 내지 않는다.** 화면에
샘플은 **하나**이고 CTA도 **하나**다(001 결정 8).

**2 — 첫날 홈에서 "없다"고 말하는 블록을 하나로 줄인다.** 방법은 접는 것이다
(`SKILL.md:58-60`의 `partial` 규칙). 세션 기록이 하나도 없으면 활동 카드 두
장이 같은 말을 두 번 하고, 상태 타일 셋이 전부 0이면 그 줄은 아직 아무것도
가리키지 않는다. **무엇을 접을지는 구현이 정해도 된다** — 내가 정하는 것은
개수다.

**단, 복귀 카드의 단수 `0`은 접지 않는다**(`index.tsx:250`). 그건 결핍이
아니라 006이 정한 앵커다(`006-home-resume-card.md:721`). 아래 관문 B의 세는
규칙에서 명시적으로 뺐다.

**3 — 필터 결과 0과 데이터 0을 가른다. 그리고 버튼을 준다.** 지금 문구는
프로젝트를 넷 가진 사용자에게 "아직 프로젝트가 없어요"라고 말한다. 그리고
`SKILL.md:129`의 체크리스트("빈 상태에 다음 행동으로 가는 버튼이 하나 있다")를
못 넘는다 — 아이콘 버튼은 헤더에 있고, 005 검증이 실제로 그것을 한 번 못
찾았다(`005-project-image-cards.md:207-208`).

**4 — F.** 아래 관문 A가 이 항목의 전부다.

#### 관문 A — 빈 윗면의 밀도. 두 값을 함께 잰다

`scripts/shot-cards.mjs`가 이미 이 카드를 심는다 — `:62`의 p3
`"이름만 있는 목도리"`(사진 없음·실 없음). 재는 자리가 이미 있다.

**A1. 잉크 비율 ≥ 3.0%.**
윗면을 캔버스로 되읽어, 바탕색과 채널 차가 **6/255**를 넘는 픽셀의 비율을 낸다.
005 검증이 채도·밝기·국소 대비를 낸 방법과 같은 계열이다
(`005-project-image-cards.md:241-244`).

기준점은 **남의 판정에 붙인다.** 실 색 스와치가 있는 카드의 스와치는 96×64이고
윗면은 356×267이라 **6.46%**다. 005에서 기획과 검증이 둘 다 이 밀도를 보고
"경계를 넘지 않았다"고 판정했다(`005-project-image-cards.md:253-257`,
`:386-388`). **그 절반을 바닥으로 둔다.** 3.0%는 잰 값이 아니라 그 값에서 내가
고른 선이다(규칙 3).

지금 값은 **약 0.34%다 — 산수이고, 재본 것이 아니다.** 가정을 적는다: 선 굵기
1px, 점선이 둘레의 절반, 눈금이 267px에 16줄
(`card-cover.tsx:46`의 `15px/16px` 반복). 눈금 160px² + 점선 둘레 320px²의
절반 160px² = 320px²를 356×267=95,052px²로 나눈 값이다.

**A2. 잉크가 닿은 세로줄이 윗면 폭의 60%를 넘지 않는다.**
A1만 두면 005가 한 번 실패한 자리로 되돌아간다. 구현이 그때 가로줄을 면 전체에
12px 간격으로 깔았다가 **줄공책**이 됐다고 적었다
(`005-project-image-cards.md:125-128`). 그 실패는 잉크의 **양**이 아니라
**퍼진 범위**였다 — 산수로는 8.2%라 A1의 바닥은 넘는다. 그러니 양만 재면 못
잡는다.

잉크 픽셀이 하나라도 있는 x열의 수를 폭으로 나눈다. 지금 구조는 눈금 10px +
점선 96px이라 약 **30%**이고, 버려진 12px 가로 괘선은 **100%**다(둘 다 산수).
**이 관문은 마지막 단에만 건다** — 사진과 도면은 원래 폭을 채운다.

**A1이 무채 구조만으로 닿는 값인지는 내가 모른다.** 아래에서 구현에게 묻는다.
**색으로 채우는 길은 막혀 있다** — 이 카드는 실이 없어서 칠할 색 자체가 없고,
`docs/DESIGN.md:130`이 임의의 색을 UI가 만드는 것을 금지한다.

#### 관문 B — 홈을 두 상태로 띄워 센다

**B1. 프로젝트 0개.** 샘플 카드가 **정확히 1개**, CTA가 **정확히 1개**. 샘플을
누르면 `/projects/new`로 가고 **아무것도 저장되지 않는다.**

**B2. 첫날(프로젝트 1개·계획중·사진/실/카운터/세션 없음).** 결핍 블록이
**1개 이하.** 지금 5개다(코드에서 센 값 — 재지 않았다).

**세는 규칙을 못 박는다. 구현이 바꾸지 않는다.** 탭바 위에 그려진 것 중,

- (a) 본문이 `없어요`로 끝나는 안내 블록
- (b) 값이 정확히 `0`인 통계 타일(`index.tsx:118-136`)

둘의 합이다. **복귀 카드의 단수는 세지 않는다** — 타일이 아니고 006의
앵커다. 기준 언어는 한국어다(`src/i18n/ui/ko.ts`).

**문구를 바꿔서 이 관문을 피하지 않는다.** `없어요`를 다른 말로 바꾸면 세는
규칙도 함께 고치고 **그 사실을 스레드에 적는다.** 안 적고 문구만 바꾸면 관문을
통과한 것이 아니라 관문에서 숨은 것이다.

#### 깨뜨리기 — 절차이지 선택이 아니다

`docs/WORKLIST.md:493-508`의 "반복되는 실패 하나"다. 같은 종류가 세 번 났고
셋 다 화면에서는 정상과 구분되지 않았다.

- **A1·A2·B1·B2를 각각 일부러 깨뜨려 종료코드 1을 확인한다.**
- **깨뜨렸는데 0이 나오면 관문이 튼튼한 것이 아니라 깨뜨리기가 실패한
  것일 수 있다.** 006에서 검증이 `CoverThumb`에 `className`을 줬는데 그
  컴포넌트가 받지 않아 소품이 조용히 버려졌고 종료코드 0이 나왔다
  (`006-home-resume-card.md:396-401`, `cover-thumb.tsx:11-21`). **어긋냈다는
  사실 자체를 먼저 확인하고, 어떻게 확인했는지를 적는다.**
- **환경·구조 문제는 종료코드 2로 가른다.** 005·006이 이미 그렇게 갈랐다
  (`shot-cards.mjs:16`, `measure-safe-area.mjs:24-25`). 003에서 종료코드 1이
  관문 실패가 아니라 모듈 로드 실패였던 일이 있다.

#### 완료 기준

1. **홈(프로젝트 0개)**: 샘플 카드 1개 + CTA 1개. 샘플은 누르면
   `/projects/new`로 가고 아무것도 저장하지 않는다. **편물 사진을 흉내 내지
   않는다.**
2. **첫날 홈**: 결핍 블록 1개 이하(위 세는 규칙).
3. **목록 빈 상태**: 필터 결과 0과 데이터 0이 서로 다른 문구이고, 다음 행동으로
   가는 버튼이 **글자와 함께** 하나 있다.
4. **카드의 빈 윗면**: A1 ≥ 3.0%, A2 ≤ 60%.
5. **관문 A·B가 `scripts/` 안에서 종료코드로 나온다.** 관문 실패 1, 환경·구조
   문제 2.
6. **넷 다 깨뜨려보고, 깨뜨린 방법이 실제로 먹혔다는 것까지 적었다.**
7. **003·004·005·006 관문이 여전히 종료코드 0.** 006의 관문 0은 자기 DB를
   심으므로(`measure-safe-area.mjs:88-130`) 홈 빈 화면을 바꿔도 영향을 받지
   않아야 한다 — **그것도 확인한다.**
8. `npm run test` · `type-check` · `lint` · `build` 종료코드 0.
9. **규칙 7 — 결정이 `docs/DESIGN.md`로 나간다.** 특히 **마지막 fallback 단의
   밀도 규칙(A1·A2)**을 §5에 적는다. `docs/DESIGN.md:295-298`이 지금 사슬의
   순서만 적고 마지막 단이 얼마나 진해야 하는지는 안 적는다. **안 적으면 다음
   사람이 이 윗면을 다시 비운다.** 015가 태어난 이유가 그것이다.

#### 안 할 것

- **프로젝트 상세** — 008(복귀 브리핑)·009(빈 섹션 접기)다. 007이 손대면 세
  작업이 한 브랜치에 섞인다.
- **`StartGuide` 재설계** — 이미 한 번에 한 걸음만 말하고 카운터가 생기면
  접는다(`src/features/project/components/start-guide.tsx:24-30`, `:55-57`).
  `WORKLIST.md:235`의 "한 번에 강요하지 않는다"는 **이미 되어 있다.**
  007이 다시 만들 이유가 없다.
- **로딩 화면** — `src/routes/index.tsx:41`이 `return null`이고 `SKILL.md:50`이
  그것을 금지한다(앱 전체에 56곳). **007은 빈 화면이지 로딩 화면이 아니다.**
  적어두고 넘긴다 — 큐 규율대로 앞에 앉히지 않는다(`WORKLIST.md:531-556`).
  정비 배치(O)의 후보이지만 **사용자 화면을 바꾸므로** 그 절의 기준에는 걸린다.
  어디에 둘지는 이 작업이 정할 것이 아니다.
- **넓은 화면** — 011. 관문은 폰에서만 잰다.
- **강조색이 셋인 문제** — 017이다. 006이 적어두고 넘겼다.

#### 내가 다시 반대할 조건

1. **빈 윗면이 또 다음으로 미뤄지는 것.** 이번에는 미룰 자리가 없다.
2. **윗면에 사진을 흉내 낸 그림이나 카테고리 일러스트가 들어오는 것.**
   그건 선택지 C·I이고 013의 자리를 미리 먹는다.
3. **관문이 문장으로만 남는 것.** 005에서 내가 같은 실수를 했고, 검증이
   "문장으로 뒀으면 006에서 아무도 안 돌렸을 것"이라고 적었다
   (`005-project-image-cards.md:550-552`).
4. **첫날 홈의 결핍 블록을 줄이겠다고 복귀 카드의 단수 `0`을 감추는 것.**
   숫자를 감추는 것은 접는 것이 아니다.

**넘김: 구현** — 묻는 것 넷.

1. **A1의 3.0%가 무채 구조만으로 닿는 값인가.** 산수로 지금이 0.34%이고 실
   스와치가 6.46%다. 1px 선만으로 3%를 채우려면 A2가 허용하는 폭(356×0.6 ≈
   213px) 안에서 선이 열세 줄쯤 필요한데(213×267 영역, 산수), **그 열세 줄이
   다시 줄공책이 되지 않나.** 안 된다고 보면 숫자를 달아 되받아라 — **3.0%를
   내리는 것도, A2를 다른 축으로 바꾸는 것도 답이다.** 관문 값은 내가 정하지만,
   **닿을 수 없는 값을 고집하면 관문이 아니라 벽이다.**
2. **잉크를 어디서 어떻게 재나.** `shot-cards.mjs:25`가
   `deviceScaleFactor: 2`라 되읽는 픽셀이 네 배다. 그리고 **바탕색을 상수로
   박으면 015가 걸린 그 실패를 반복한다** — 테스트가 `index.css`를 안 읽고
   복사된 상수끼리 비교했다(`WORKLIST.md:500`). 실제 계산 스타일에서 읽어올 수
   있나.
3. **관문 B를 어느 스크립트에 두나.** `measure-safe-area.mjs`가 이미 홈에 DB를
   심지만(`:88-130`) 그 DB는 사진이 있는 프로젝트다. 같은 스크립트에 상태를
   둘 더 다는 것과 새 스크립트를 만드는 것 중 **어느 쪽이 싼지는 네가 안다.**
4. **홈 빈 화면의 샘플을 `ResumeCard`(`index.tsx:177`)로 그릴 수 있나.**
   재사용하면 006이 정한 모양이 자동으로 따라온다. 그런데 샘플이 진짜 카드와
   같은 DOM이 되면 **관문들이 샘플을 첫 카드로 잡을 수 있다** —
   `measure-safe-area.mjs`가 "이어서 뜨기 절의 카드"로 첫 카드를 찾는다(`:134`).
   따로 그려야 한다고 보면 그 값도 함께 말해달라.

## 결정
